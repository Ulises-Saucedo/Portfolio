---
import Layout from "@/layouts/Layout.astro";
import data from "@/data/data.json";
import works from "@/data/works.json";
import Card from "@/components/Card.astro";
import WorkCard from "@/components/WorkCard.astro";
import Contact from "@/components/Contact.astro";
---

<Layout title="Bienvenido a mi portfolio | Ulises Saucedo">
  <div class="max-w-[800px] mx-auto p-5 mt-40 md:mt-56 font-poppins">
    <h1 class="text-5xl font-bold text-white">
      Hey 👋, soy <span class="text-green-400">Ulises</span>
    </h1>
    <p class="mt-4 text-white opacity-50 text text-pretty">
      Desarrollador Fullstack, apasionado por el aprendizaje continuo.
      Actualmente, estoy enfocado en encontrar una posición como Programador Web
      Frontend Junior o Backend Junior. Dedico mis momentos libres a contribuir
      en proyectos colaborativos de código abierto, donde puedo aplicar y
      ampliar mis habilidades.
    </p>
    <div class="mt-4">
      <Contact text="Github" href="https://github.com/Ulises-Saucedo" />
      <Contact text="CV" href="src/assets/CV-Ulises-Saucedo.pdf" />
      <Contact text="Correo" href="mailto:ulisesaucedo.se04@gmail.com" />
      <Contact
        text="Linkedin"
        href="https://www.linkedin.com/in/ulises-saucedo/"
      />
    </div>
  </div>

  <section class="max-w-[800px] mx-auto p-5 font-poppins my-12">
    <h3 class="mb-4 text-green-300 font-bold text-xl">Experiencia laboral</h3>
    <div class="space-y-4">
      {
        works.works.map((work) => (
          <WorkCard
            title={work.title}
            date={work.date}
            description={work.description}
          />
        ))
      }
    </div>
  </section>

  <section class="max-w-[800px] mx-auto p-5 font-poppins my-12">
    <h3 class="mb-4 text-green-300 font-bold text-xl">Mis proyectos</h3>
    <section class="gap-4 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3">
      {data.projects.map((project) => <Card project={project} />)}
    </section>
  </section>
</Layout>
